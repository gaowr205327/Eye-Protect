# 统一文本/二进制判定与换行策略
* text=auto

# ---------------------------------------------------------------------------
# Windows 批处理：保持原始字节，禁止任何换行或编码转换
#
# build.bat 以 GBK/CP936 编码保存，并在开头显式 chcp 936 —— 因为 cmd 会按
# 「当前代码页」解码批处理文件。若文件被换成 UTF-8 或 CRLF 被改写，中文行与
# 参数都会解码错乱（实测会让 PyInstaller 收到乱码名称，产物变成 0 字节）。
# 因此这类文件必须逐字节原样保存。
# ---------------------------------------------------------------------------
*.bat -text
*.cmd -text

# 二进制资源：禁止任何转换
*.png  binary
*.ico  binary
*.ttf  binary
*.otf  binary
*.exe  binary
*.dll  binary
*.zip  binary
