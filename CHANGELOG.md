# 变更记录（CHANGELOG）

本文件记录 **护眼助手（Eye-Protect）** 的版本变更。
版本号与设置页「关于 · 护眼助手 vX.Y」中显示的一致（`src/settings_window.py` 的 `APP_VERSION`）。

---

## v1.9 — 2026-09-17

**新增**

- 休息弹窗增加第二个按钮「稍后 N 分钟」（`defer_minutes`，1~60，默认 5）。
  点击后本次提醒作废，休息计时从当前时刻重新起算 N 分钟；该延后值**只生效一次**，
  下一次恢复为正常间隔。打卡弹窗不加此按钮（打卡是"到点就该做"的事，延后无意义）。
- 设置页新增「弹窗个数」（1~6，默认 1）：一次提醒可同时弹出多个弹窗，铺在桌面
  **互不重叠**的不同位置，点击其中任意一个即关闭全部同批弹窗。
  个数为 1 时行为与旧版完全一致。

**修复**

- 主功能页「快捷设置」的绿色高亮在界面首绘后不再更新 —— 点「60分」数字是 60、
  高亮却留在 25 分上。现在高亮由专门的刷新函数在间隔值变化时重算；
  输入非快捷值（如 37 分钟）时四个按钮全不亮，不再出现假选中。

**打包**

- 修正 `build.bat` 中字体资源使用相对路径的问题：PyInstaller 6.21 会把相对路径
  解析到 spec 文件所在目录，导致打包报错。已改为绝对路径。

---

## v1.8 — 2026-09-17

- 弹窗外形改为**程序化像素木牌**（阶梯圆角木框 + 顶部草绿色带 + Bayer 抖动羊皮纸内胆 +
  铆钉与角落草花装饰），四个角真正镂空（`-transparentcolor`）。
- 动画由补间改为**阶梯帧**（8~12 fps），贴合像素风手感。

---

## v1.7 — 2026-09-17

- **界面整体改为星露谷（Stardew Valley）田园像素风**：全部组件由程序化 2D 像素绘制
  （阶梯圆角 / 厚木框 / 铆钉 / 木纹 / Bayer 有序抖动渐变 / 16px 手绘图标 /
  草簇·小花·嫩芽·蘑菇·金币等装饰件），**零外部图片素材**。业务逻辑未改动。
- **字体换成开源像素字体** Fusion Pixel 12px（OFL 1.1，可商用）：随包分发，
  仅在进程内私有注册（不写注册表、不装进系统），文件缺失或不可用时自动回落系统字体。
- **皮肤系统精简为单套**（星露谷）：原 5 套预设删除，保留 `theme.SKINS` 键与
  合法性校验接口以兼容旧配置。
- **新增上下班打卡提醒**：每日 4 次（上班 / 午休 / 午休结束 / 下班），
  每条可独立开关、自定义时刻与提示词；旧版「下班提醒」配置自动迁移到第 4 条。
- 主界面改为**三标签页**：主功能 / 打卡提醒 / 设置。
- 应用图标由 👍 改为**像素眼睛**（草地绿圆底 + 木质描边）。

---

## v1.6

- 新增皮肤系统：5 套预设皮肤（经典蓝 / 暗夜黑 / 清新绿 / 少女粉 / 暖阳橙），
  设置界面一键切换、即时生效并自动保存，同时覆盖主界面与提醒弹窗。
  *（v1.7 已精简为单套）*
- 主界面组件外观美化：圆角卡片、主题化输入控件（Spinbox / Entry 的底色、文字、
  选中、聚焦边框全随皮肤）、统一主题配色。

---

## v1.5

- 弹窗位置改为**固定屏幕正中央**，落点按当前屏幕尺寸实时计算
  （自适应不同分辨率 / DPI / 单双屏切换）。撤销 v1.4 的拖拽记忆方案。

---

## v1.4

- 支持自定义弹窗位置并记忆拖拽结果。*（v1.5 撤销）*

---

## v1.3

- 全面安全审查与加固：
  - 单实例改为**应用层握手 + 端口占用降级**（避免"端口被占即无法启动"的本地 DoS 面）
  - 配置严格类型校验（字符串 `"false"` 不得被当作 `True`）、文件大小上限 1 MB、
    原子写（临时文件 + `fsync` + `os.replace`）、损坏自动备份 `.bak`
  - 事件轮询异常隔离，单个事件失败不影响后续
  - 注册表句柄用 `finally` 确保关闭；弹窗文本清洗

---

## v1.2

- 去除软件与弹窗中的所有眼睛图标（设置窗口标题 / 预览按钮 emoji、弹窗眼睛徽章），
  弹窗改为无徽章、标题居中。
- 休息弹窗按钮文字（原「知道了，继续干活」）改为可自定义。

---

## v1.1

- 弹窗配色改版（白色 + 胖次蓝、黑字），修复顶部圆角色带缺陷。
- 图标改为 👍，软件名称改为「护眼助手」。
- 新增下班提醒功能。
